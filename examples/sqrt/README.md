# Optional sample tree

Not the default Choreo job. Paste this folder if you want a pre-existing `integerSqrt` tree. The dashboard starts **empty** and writes tests alongside implementation unless you point it at a folder that already has tests.
